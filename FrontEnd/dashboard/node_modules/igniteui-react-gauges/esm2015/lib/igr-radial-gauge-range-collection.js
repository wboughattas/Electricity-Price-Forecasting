import { IgCollection } from "igniteui-react-core";
import { markType } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { XamRadialGaugeRange as XamRadialGaugeRange_internal } from './XamRadialGaugeRange';
import { IgrRadialGaugeRange as IgrRadialGaugeRange } from './igr-radial-gauge-range';
export class IgrRadialGaugeRangeCollection extends IgCollection {
    constructor(list) {
        super();
        if (!IgrRadialGaugeRange.$type) {
            IgrRadialGaugeRange.$type = markType(IgrRadialGaugeRange, "IgrRadialGaugeRange");
        }
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _createInnerColl() {
        if (!IgrRadialGaugeRange.$type) {
            IgrRadialGaugeRange.$type = markType(IgrRadialGaugeRange, "IgrRadialGaugeRange");
        }
        let coll = new SyncableObservableCollection$2(IgrRadialGaugeRange.$type, XamRadialGaugeRange_internal.$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext._implementation;
        };
        coll.createFrom = (int) => {
            let ext = int.externalObject;
            if (!ext) {
                ext = IgrRadialGaugeRange._createFromInternal(int);
                if (ext) {
                    ext._implementation = int;
                }
            }
            return ext;
        };
        return coll;
    }
}
//# sourceMappingURL=igr-radial-gauge-range-collection.js.map
